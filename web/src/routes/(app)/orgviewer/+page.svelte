<!-- Unreachable in normal operation: +page.server.ts's load always throws
     a redirect (or a 500 if PUBLIC_ORGVIEWER_ROOT_UUID is unset). A
     +page.svelte is still required for SvelteKit to treat this directory
     as a page route at all. -->

