<!-- Tree-only view: the tree itself is rendered by the (tree-shell) layout;
     there's nothing further to show here until an org unit or person is
     selected (see the sibling orgunit/ and person/ routes). -->
