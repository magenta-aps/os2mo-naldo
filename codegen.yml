schema: "http://127.0.0.1:5001/graphql/v3"
documents:
  - "**/*.{graphql,gql}"
generates:
  src/lib/graphql/types.ts:
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
    config:
      useTypeImports: true
